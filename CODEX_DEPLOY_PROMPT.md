# Codex 執行任務：上傳卡之洞預購 HTML 原型

## 1. 任務目的與優先順序

使用者要把現成的 HTML 原型上傳到 GitHub，供後續展示與討論。本次只做封裝檔案的版本控制與靜態展示部署；不要重新開發訂購系統。

1. 驗證原始檔與登入環境。
2. 將檔案上傳到獨立儲存庫。
3. 在已獲授權、可見性及帳號功能允許時設定 GitHub Pages。
4. 驗證實際提交及可用頁面，回報結果。

主要成功條件是「GitHub 中確實有正確的 HTML 檔案及提交」。Pages 是後續展示步驟，不能把未完成 Pages 說成檔案上傳失敗，也不能把成功上傳說成網站已可開啟。

## 2. 固定目標

- 預期 GitHub 帳號：`CyanYu5`。
- 儲存庫：`CyanYu5/cardscave-preorder-preview`。
- 入口檔案：根目錄 `index.html`。
- 預期部署來源：`main` 分支根目錄；若既有儲存庫採不同方式，先檢查，不擅自改掉既有設定。
- 目前資料夾：`cardscave-preorder-preview`。

### 絕對禁止

- 不得修改 `CyanYu5/farmhouse-preview`，它是另一個農舍展示專案。
- 不得修改卡之洞正式會員系統、正式伺服器、資料庫、DNS 或正式網域。
- 不得把檔案推送到其他帳號／儲存庫，也不得覆寫不相關檔案。
- 不得強制推送、刪除歷史紀錄、刪除既有分支或移除他人變更。
- 不得要求使用者把 GitHub Token、密碼或 SSH 私鑰貼在聊天中。
- 不得將憑證、`.env`、實際訂單、匯款證明或其他個資提交到儲存庫。
- 不得自行將私人儲存庫轉為公開。

## 3. 本次包含的檔案

- `index.html`：既有原型，保留內容不變，不重寫版面與功能。
- `cardscave_preorder_plan.md`：原開發草案，只供背景閱讀；不要依此展開功能開發。
- `README.md`：封裝使用說明。
- `CODEX_DEPLOY_PROMPT.md`：本次執行規格。
- `.nojekyll`、`.gitignore`、`SHA256SUMS.txt`。

若發現現有 HTML 本身有缺陷，先記錄，不默默修正。部署設定若確實需要新增檔案，僅在目標儲存庫新增最少必要檔案，回報變更及理由。不要為單檔 HTML 加入不必要的框架、套件、建置步驟或付費服務。

## 4. 執行步驟

### A. 本機與檔案檢查

- 確認 `index.html` 與原開發計畫存在且非空。
- 依 `SHA256SUMS.txt` 驗證檔案完整性；不一致時先查清原因，不能直接忽略。
- 檢查 HTML 是否意外含有密鑰、真實收款帳號或真實會員資料；公開發布前發現可疑內容時先報告，不自行清除或外傳。
- 檢查工作目錄是否已在另一個 Git 儲存庫之內。必要時建立隔離的工作目錄，不改動父層專案。
- 如可使用瀏覽器測試，確認 HTML 可以載入，記錄明顯的主控台錯誤；不要把外部圖片服務不可用誤判為 GitHub 上傳錯誤。

### B. GitHub 授權

- 使用本機已授權的 Git／GitHub CLI，先檢查工具是否存在與登入狀態。
- 可先使用 `gh auth status` 檢查；避免輸出或列印 Token。
- 核對目前登入帳號是否是 `CyanYu5`。若不是，不推送，請使用者選擇正確帳號。
- 尚未登入時，讓使用者透過 GitHub 正式瀏覽器登入／授權流程處理；例如環境支援時使用 `gh auth login --web --git-protocol https`。
- 如目前 Codex 執行環境沒有必要的網路、工具或登入能力，回報精確障礙及需要使用者完成的最小步驟。不要一再重試相同的 403。
- 不要依賴先前 ChatGPT 對話裡的 GitHub 連接器；那條連線曾回覆 `403 Resource not accessible by integration`。本次要驗證的是此執行環境自身的實際寫入授權。

### C. 目標儲存庫與可見性

- 先檢查 `CyanYu5/cardscave-preorder-preview` 是否存在。
- 如果已存在：查看可見性、預設分支、remote、現有檔案與近期提交，再決定安全的追加方式。
- 若已存在同名 `index.html`，先比對：相同則不重複覆寫；不同則列出差異，除非本次工作環境已有使用者明確覆寫授權，否則只針對此衝突請使用者確認。
- 如果不存在：建立這個獨立儲存庫，但公開可見性尚未在本任務中取得明確核准。先用一個必要問題確認使用者要公開展示，還是僅私人保存，不要擅自選公開。
- 保持使用者指定的可見性。若私人儲存庫無法依帳號功能啟用 Pages，完成私人上傳並報告限制；不要自行公開、升級方案或轉用其他平台。

### D. 上傳

- 將原型相關檔案放在目標儲存庫根目錄，不要讓 ZIP 外層資料夾造成入口多嵌套一層。
- 提交前只加入本次必要檔案，不要盲目把整台電腦或上層資料夾加入版本控制。
- 驗證 remote 明確指向 `CyanYu5/cardscave-preorder-preview` 後再推送。
- 使用清楚的提交訊息，例如 `Add Cardscave preorder HTML prototype`。
- 成功後讀回遠端提交／檔案，確認遠端 `index.html` 的位元組內容或 SHA-256 與包內相同。

### E. GitHub Pages（具備授權與功能時）

- 在目標儲存庫內設定或沿用適當的靜態頁面部署方式，優先使用分支根目錄，避免不必要的建置流程。
- `.nojekyll` 已附上；不要因此修改 HTML。
- 若啟用 Pages 所需權限不足，精確區分「Contents 寫入」「儲存庫設定」「Pages 設定」的失敗步驟。
- 不得只根據帳號及專案名稱拼接網址，就宣稱頁面已可用；應讀取實際 Pages 設定或部署結果。
- 在當次可用執行時間內檢查發布狀態與頁面回應；不可無限等待或宣稱會在背景完成。
- 若仍在建置中，回報「已上傳、Pages 尚在部署」，附實際可查驗的部署紀錄，不編造成功結果。

## 5. 驗收

| 項目 | 必須看到的證據 |
|---|---|
| 正確目的地 | GitHub remote 與實際提交位於 `CyanYu5/cardscave-preorder-preview`。 |
| 檔案未改動 | 遠端 HTML 與包內 `index.html` 內容校驗一致。 |
| 可追溯提交 | 提交 SHA、提交連結與 HTML 檔案連結。 |
| 其他專案未變動 | 只在獨立目標儲存庫寫入，沒有操作 `farmhouse-preview`。 |
| 網站發布 | 若已完成 Pages，提供真實部署狀態與可開啟的網站連結。 |
| 介面檢查 | 有瀏覽器工具才實測桌面／手機寬度與用戶端／管理端切換；無工具需註明未測試。 |
| 原型界線 | 不宣稱有正式登入、共享資料庫、Email／LINE 通知、付款或物流串接。 |

## 6. 最後如何回覆使用者

先說明「上傳成功／失敗」與「網站已發布／未發布」兩個獨立結果，再提供：

1. 儲存庫連結、HTML 檔案連結、提交連結或 SHA。
2. 已驗證可用的展示網址（僅限確實完成發布）。
3. 本次新增或更改了什麼；HTML 是否保持原樣。
4. 實際執行的驗證及未完成事項。

遇到權限錯誤，記錄操作名稱、狀態碼和非機密錯誤內容，只提出該障礙所需的一項最小修正。不要把推測當成已確認原因，也不要再把 GitHub 工具存在等同於已有寫入權限。
